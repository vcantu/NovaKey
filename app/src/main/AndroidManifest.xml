<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="viviano.cantu.novakey"
    android:versionCode="22"
    android:versionName="0.3.7.1beta">
    <!-- WARNING!!!!!!!! THIS VERSION CODE MUST EQUAL VERSION CODE IN SETTINGS -->

    <uses-sdk
        android:minSdkVersion="11"
        android:targetSdkVersion="23" />

    <uses-permission android:name="android.permission.SYSTEM_ALERT_WINDOW" />
    <uses-permission android:name="android.permission.VIBRATE" />

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE"/>

    <application
        android:allowBackup="true"
        android:icon="@drawable/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/AppTheme">

        <!-- Declares the input method service -->
        <service
            android:name=".NovaKey"
            android:label="@string/app_name"
            android:permission="android.permission.BIND_INPUT_METHOD"
            android:theme="@style/AppTheme">
            <intent-filter>
                <action android:name="android.view.InputMethod" />
            </intent-filter>

            <meta-data
                android:name="android.view.im"
                android:resource="@xml/method"
                android:theme="@style/AppTheme"/>
        </service>

        <activity
            android:name=".setup.SetupActivity"
            android:icon="@drawable/ic_launcher"
            android:label="@string/app_name" />
        <activity
            android:name=".settings.SettingsActivity"
            android:icon="@drawable/ic_launcher"
            android:label="@string/app_name"
            android:theme="@style/AppTheme">
            <intent-filter>
                <action android:name="android.intent.action.CREATE_SHORTCUT" />
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".setup.TutorialActivity"
            android:theme="@style/TutorialTheme"
            android:configChanges="orientation|keyboardHidden"
            android:icon="@drawable/ic_launcher"
            android:label="@string/app_name"
            android:windowSoftInputMode="adjustResize" />
        <activity
            android:name=".settings.StylePreferenceActivity"
            android:label="@string/title_style_preference"
            android:hardwareAccelerated="true">
        </activity>

        <activity
            android:name=".tests.EmojiSettingActivity"
            android:hardwareAccelerated="false" />
    </application>

</manifest>
